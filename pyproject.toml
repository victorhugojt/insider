[project]
name = "insider"
version = "0.1.0"
description = "Local activity recommendation multi-agent system"
requires-python = ">=3.14"
dependencies = [
    "google-adk>=1.30.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.uv]
package = false
